services:
  chess:
    build: .
    ports:
      - "8002:8000"
    volumes:
      - chess_data:/data
      - ./app:/srv/app
    restart: unless-stopped
    env_file:
      - .env
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000

volumes:
  chess_data:
